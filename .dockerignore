# Dependencies — installed fresh inside the image
node_modules
**/node_modules

# Secrets — DB credentials are passed at `docker run` time via -e, never baked in
.env
**/.env
.env.*
**/.env.*

# Version control
.git
.gitignore

# Logs
npm-debug.log*
**/npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS junk
.DS_Store
Thumbs.db

# Docs / editor cruft not needed inside the image
docs
README.md
.vscode
.idea
