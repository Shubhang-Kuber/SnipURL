# SnipURL backend — database configuration
# Copy this file to `.env` in the same directory and fill in DB_PASSWORD.
# `.env` is gitignored; this file is safe to commit.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=snipurl
DB_PORT=3306
